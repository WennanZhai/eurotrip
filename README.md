# Eurotrip

一个轻量、响应式的欧洲旅行主题静态网站，无需构建步骤，可直接部署到 Cloudflare Workers / Pages。

## 本地预览

```bash
python3 -m http.server 8787
```

然后访问 `http://localhost:8787`。

## 部署

在 Cloudflare 中连接此 GitHub 仓库：

- 构建命令：留空
- 构建输出目录：`/`

## 技术栈

- HTML5
- CSS3
- Vanilla JavaScript
